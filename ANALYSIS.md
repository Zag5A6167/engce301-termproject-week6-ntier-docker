# 📊 การวิเคราะห์เปรียบเทียบ: VM vs Docker Deployment
## ENGCE301 - Week 6 N-Tier Architecture

**ชื่อ-นามสกุล:** [ชาคริตส์ แก้วมูลเมือง]  
**รหัสนักศึกษา:** [67543206005-0]  
**วันที่:** 26 กุมภาพันธ์ 2026

---

## 1. ตารางเปรียบเทียบ Setup Process

| ขั้นตอน | Version 1 (VM) | Version 2 (Docker) |
|---------|----------------|-------------------|
| ติดตั้ง PostgreSQL | ต้องรันคำสั่ง apt-get และ config ทีละขั้นตอน | ใช้ Image `postgres:16-alpine` รันได้ทันที |
| ติดตั้ง Node.js | ต้องติดตั้ง nvm/node และ npm install เอง | มีการ Build ผ่าน `Dockerfile` สภาพแวดล้อมคงที่ |
| ติดตั้ง Nginx | ติดตั้งผ่าน OS และแก้ไขไฟล์ใน /etc/nginx | รันผ่าน Image `nginx:alpine` และ Mount config เข้าไป |
| Configure Database | ต้องรัน script สร้าง table ผ่าน psql manually | ใช้ `init.sql` ใน `docker-entrypoint-initdb.d` อัตโนมัติ |
| Configure SSL | ต้องเจนไฟล์และวางในพาธที่กำหนดเองบน Host | เจนไฟล์ครั้งเดียวแล้ว Mount เข้า Container พาธไหนก็ได้ |
| Start Services | ต้องใช้ systemctl start ทีละตัว | ใช้ `docker compose up -d` สั่งรันได้ทั้งระบบ |
| **เวลาทั้งหมด** | ประมาณ 30-45 นาที | ประมาณ 5-10 นาที |

---

## 2. ตารางเปรียบเทียบ Resource Usage

| Resource | Version 1 (VM) | Version 2 (Docker) |
|----------|----------------|-------------------|
| Memory Usage | สูง (เพราะต้องรัน Full OS สำหรับแต่ละ VM) | ต่ำ (Share OS Kernel ใช้เท่าที่ Service ต้องการ) |
| Disk Usage | สูง (GB+) | ต่ำ (MB ตามขนาด Image และ Volumes) |
| CPU Usage | มี Overhead จากการจำลอง Hardware | แทบไม่มี Overhead (Native Performance) |
| Startup Time | ช้า (หลักนาที) | เร็ว (หลักวินาที) |

---

## 3. ข้อดีของ Docker Deployment

1. **Environment Consistency:** มั่นใจได้ว่ารันบนเครื่องไหนก็เหมือนกัน (เครื่องเพื่อน/เครื่องเซิร์ฟเวอร์)
2. **Easy Scaling:** สามารถเพิ่มจำนวน Instance ของ API ได้ง่ายเพียงแค่ปรับ `replicas`
3. **Isolation:** แต่ละ Service แยกออกจากกันชัดเจน ปัญหาของตัวหนึ่งไม่กระทบ OS หลัก
4. **Portability:** ย้ายโปรเจกต์ไปเครื่องใหม่ได้ทันทีเพียงแค่มีไฟล์ `docker-compose.yml`
5. **Fast Recovery:** หาก Service พัง Docker สามารถสั่ง `restart: always` ให้กลับมาทำงานได้เองอัตโนมัติ

---

## 4. ข้อเสียของ Docker Deployment

1. **Networking Complexity:** การสื่อสารระหว่าง Container อาจซับซ้อน (เช่น ปัญหา localhost vs 0.0.0.0)
2. **Storage Persistence:** หากไม่ทำ Volumes ข้อมูลจะหายทันทีที่ลบ Container
3. **Security Monitoring:** ต้องหมั่นอัปเดต Base Image เพื่อปิดช่องโหว่ความปลอดภัย

---

## 5. เมื่อไหร่ควรใช้ VM vs Docker?

### ควรใช้ VM เมื่อ:
- ต้องการรัน App ที่ต้องการ Kernel เฉพาะตัวหรือ OS ที่ต่างกันชัดเจน
- ต้องการความปลอดภัยระดับ Hardware Isolation
- ระบบเก่า (Legacy) ที่ไม่รองรับการทำงานแบบ Microservices

### ควรใช้ Docker เมื่อ:
- พัฒนาเว็บแอปพลิเคชันยุคใหม่ (Modern Web App)
- ต้องการทำ CI/CD ที่รวดเร็วและเป็นอัตโนมัติ
- ต้องการประหยัดทรัพยากรเครื่องเซิร์ฟเวอร์ (High Density)

---

## 6. สิ่งที่ได้เรียนรู้จาก Lab นี้

ได้เรียนรู้การทำ N-Tier Architecture โดยใช้ Docker Compose ในการจัดการความสัมพันธ์ระหว่าง Database, Backend และ Proxy ซึ่งช่วยลดความยุ่งยากในการติดตั้ง Environment นอกจากนี้ยังได้ฝึกแก้ปัญหาเรื่อง Network ภายใน Docker และการจัดการไฟล์ Config ของ Nginx ให้ทำงานร่วมกับ SSL ได้อย่างถูกต้อง

---

## 7. คำสั่ง Docker ที่ใช้บ่อย (Quick Reference)

```bash
# เริ่มต้นระบบทั้งหมดแบบ Background
docker compose up -d

# หยุดการทำงานและลบ Container ทั้งหมด
docker compose down

# ดูสถานะและ Healthcheck ของแต่ละเครื่อง
docker compose ps

# ดู Log ของแต่ละ Service เพื่อแก้ปัญหา (เช่น api หรือ nginx)
docker logs -f [container_name]

# ตรวจสอบการใช้ทรัพยากร Real-time
docker stats

# สั่ง Build ใหม่เมื่อมีการแก้ไขโค้ดหรือ Dockerfile
docker compose up -d --build

# ตรวจสอบความถูกต้องของ Nginx Config ภายใน Container
docker exec taskboard-nginx nginx -t













สร้างโดย: [ชาคริตส์ แก้วมูลเมือง]
ENGCE301 Software Architecture - Week 6 


### 8.2 คำสั่งสำหรับเก็บข้อมูล

```bash
# ดู memory usage ของ Docker
docker stats --no-stream

# ดู disk usage
docker system df

# ดู container sizes
docker ps -s

# ดู image sizes
docker images

# ดู network
docker network ls
docker network inspect taskboard-network
